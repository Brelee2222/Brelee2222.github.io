/*
var MenuTitle = document.getElementsByClassName("MenuTitle")[0];
var topContainerScrollFromTop = 50;
window.onscroll = function() {
    console.log(window.pageYOffset)
    if(topContainerScrollFromTop <= window.pageYOffset)
        MenuTitle.classList.add("fix");
    else 
        MenuTitle.classList.remove("fix");
}
*/